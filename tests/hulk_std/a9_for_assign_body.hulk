// A.9.2 — The literal `fact` definition uses a destructive assignment as the
// body of a `for` with no surrounding parentheses. The grammar must accept `:=`
// directly at the control-flow body level.
function fact(x) => let f = 1 in for (i in range(1, x + 1)) f := f * i;

print(fact(5)); // 120
